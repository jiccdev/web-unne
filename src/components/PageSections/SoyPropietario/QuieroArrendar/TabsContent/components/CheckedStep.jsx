import React from 'react';
import { iconsList } from '@/components/Icons';

const CheckedStep = () => {
  const { BsCheckCircle } = iconsList;
  return <BsCheckCircle className="text-xl ml-1.5" />;
};

export default CheckedStep;
